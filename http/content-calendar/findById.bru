meta {
  name: findById
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/content/1
  body: none
  auth: none
}

tests {
  test("Request accepted", function () {
    expect(res.getStatus()).to.equal(200);
  });
   
  test("should return content with id 1", function () {
    expect(res.getBody().id).to.equal(1);
  });
}

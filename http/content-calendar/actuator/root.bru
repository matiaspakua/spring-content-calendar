meta {
  name: root
  type: http
  seq: 1
}

get {
  url: http://localhost:8080/actuator
  body: none
  auth: none
}

tests {
  test("Request accepted", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

meta {
  name: findById_NotFound
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/api/content/999
  body: none
  auth: none
}

tests {
  test("Request not found", function () {
    expect(res.getStatus()).to.equal(404);
  });
}
